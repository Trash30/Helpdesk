DATABASE_URL=postgresql://helpdesk_user:password@localhost:5432/helpdesk_db
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters
PORT=3001
UPLOADS_PATH=./uploads
APP_URL=http://localhost:5173
# Optionnel — les IP privées (192.168.x.x, 10.x.x.x, 172.16-31.x.x) sont acceptées automatiquement.
# À renseigner uniquement pour des origines publiques ou un hostname fixe (ex: helpdesk.local).
# ALLOWED_ORIGINS=http://helpdesk.local
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your@email.com
SMTP_PASS=yourpassword
SMTP_FROM="Mon Helpdesk <noreply@helpdesk.com>"
